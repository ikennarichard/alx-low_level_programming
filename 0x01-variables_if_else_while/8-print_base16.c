#include <stdio.h>
/**
 * main - The starte of the program
 *
 * Description: Print no of base 16 in lowercase
 *
 * Return: 0
 */
int main(void)
{
	int num = 48;

	while (num <= 102)
	{
		putchar(num);

		if (num == 57)
			num+=39
		++num;
	}
	putchar('\n');
	return (0);
}
