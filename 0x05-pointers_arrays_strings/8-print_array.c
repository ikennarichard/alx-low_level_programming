#include "main.h"

/**
 * print_array - prints n elements of array of integers
 *
 * @n: elements input
 * @a: string input
 *
 * Return: Nothing
 */

void print_array(int *a, int n)
{
	int a;

	for (a = 0; a < n; ++a)
	{
		if(a != (n - i))
			printf("%d, ", a[i]);
		else
			printf("%d", a[i]);
	}
	printf("\n");
}
